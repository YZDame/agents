# Polymarket Agents 操作指南

## 简介

Polymarket Agents 是一个 Python 框架，用于构建在 Polymarket 预测市场上进行自动交易的 AI 代理。系统结合了：
- **LangChain** - LLM 编排
- **ChromaDB** - 向量数据库（RAG）
- **Web3** - 链上交易执行

## 快速开始

确保已完成：
- Python 3.9+ 环境
- Conda 环境 `polymarket` 已激活
- `.env` 文件已配置好 API 密钥
- 依赖包已安装（`pip install -r requirements.txt`）

## CLI 命令使用

所有命令通过以下方式执行：

```bash
python scripts/python/cli.py <命令> [参数]
```

**查看所有可用命令：**
```bash
python scripts/python/cli.py --help
```

## CLI 命令详解

### 📊 市场数据查询

#### 1. `get-all-markets` - 获取所有市场

查询 Polymarket 上的所有市场，可按价差排序筛选出最有交易机会的市场。

```bash
python scripts/python/cli.py get-all-markets --limit 10 --sort-by spread
```

**参数说明：**

| 参数 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `--limit` | int | 返回的市场数量 | 5 |
| `--sort-by` | str | 排序字段，当前仅支持 `spread` | spread |

**返回字段说明：**

- `id`: 市场唯一标识符
- `question`: 市场问题描述
- `end`: 市场结束时间（ISO 8601 格式）
- `description`: 详细描述
- `active`: 是否活跃
- `spread`: 价差（越大代表交易机会越好）
- `outcomes`: 可能的结果（如 ["Yes", "No"]）
- `outcome_prices`: 各结果当前价格

**使用示例：**

```bash
# 查看 spread 最高的前 20 个市场
python scripts/python/cli.py get-all-markets --limit 20 --sort-by spread

# 快速查看前 5 个市场
python scripts/python/cli.py get-all-markets
```

---

#### 2. `get-all-events` - 获取所有事件

获取 Polymarket 上的事件，一个事件可能包含多个相关市场。

```bash
python scripts/python/cli.py get-all-events --limit 10 --sort-by number_of_markets
```

**参数说明：**

| 参数 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `--limit` | int | 返回的事件数量 | 5 |
| `--sort-by` | str | 排序方式（`number_of_markets`） | number_of_markets |

**使用示例：**

```bash
# 查看包含最多市场的 10 个事件
python scripts/python/cli.py get-all-events --limit 10 --sort-by number_of_markets
```

---

### 📰 新闻与信息

#### 3. `get-relevant-news` - 获取相关新闻

使用 NewsAPI 搜索与关键词相关的新闻文章。

```bash
python scripts/python/cli.py get-relevant-news "比特币价格"
```

**参数说明：**

| 参数 | 类型 | 说明 |
|------|------|------|
| `keywords` | str | 搜索关键词（支持中文和英文） |

**前提条件：**
- 需要在 `.env` 中配置 `NEWSAPI_API_KEY`

**使用示例：**

```bash
# 搜索加密货币相关新闻
python scripts/python/cli.py get-relevant-news "加密货币"

# 搜索特定主题
python scripts/python/cli.py get-relevant-news "美国总统选举"
python scripts/python/cli.py get-relevant-news "Ethereum ETF"
```

---

### 🗃️ RAG 数据库管理

#### 4. `create-local-markets-rag` - 创建本地市场 RAG 数据库

将 Polymarket 的市场数据本地化，建立向量数据库用于语义检索。

```bash
python scripts/python/cli.py create-local-markets-rag ./data/markets_db
```

**参数说明：**

| 参数 | 类型 | 说明 |
|------|------|------|
| `local_directory` | str | 数据库存储路径 |

**作用：**
- 下载并索引所有市场数据
- 使用嵌入模型生成向量
- 存储到本地 ChromaDB 实例
- 加速后续查询，减少 API 调用

**使用示例：**

```bash
# 创建数据库
python scripts/python/cli.py create-local-markets-rag ./data/markets_db

# 自定义路径
python scripts/python/cli.py create-local-markets-rag ~/polymarket_rag_db
```

---

#### 5. `query-local-markets-rag` - 查询本地 RAG 数据库

基于自然语言查询，从本地数据库中检索相关市场。

```bash
python scripts/python/cli.py query-local-markets-rag ./data/markets_db "政治选举"
```

**参数说明：**

| 参数 | 类型 | 说明 |
|------|------|------|
| `vector_db_directory` | str | RAG 数据库路径 |
| `query` | str | 自然语言查询问题 |

**使用示例：**

```bash
# 查找政治相关市场
python scripts/python/cli.py query-local-markets-rag ./data/markets_db "美国大选"

# 查找加密货币相关市场
python scripts/python/cli.py query-local-markets-rag ./data/markets_db "比特币价格预测"

# 查找体育赛事
python scripts/python/cli.py query-local-markets-rag ./data/markets_db "NBA比赛"
```

---

### 🤖 AI 交易助手

#### 6. `ask-superforecaster` - 询问超级预测者

获取 AI 对特定交易的预测建议，基于超级预测者的方法论。

```bash
python scripts/python/cli.py ask-superforecaster \
  --event-title "2024美国总统大选" \
  --market-question "特朗普是否会获胜？" \
  --outcome "yes"
```

**参数说明：**

| 参数 | 类型 | 说明 |
|------|------|------|
| `--event-title` | str | 事件标题 |
| `--market-question` | str | 市场问题 |
| `--outcome` | str | 预测结果（通常是 "yes" 或 "no"） |

**使用示例：**

```bash
# 询问加密货币市场
python scripts/python/cli.py ask-superforecaster \
  --event-title "比特币减半事件" \
  --market-question "2024年底比特币是否会突破10万美元？" \
  --outcome "yes"

# 询问经济事件
python scripts/python/cli.py ask-superforecaster \
  --event-title "美联储FOMC会议" \
  --market-question "美联储是否会降息25个基点？" \
  --outcome "yes"

# 询问体育赛事
python scripts/python/cli.py ask-superforecaster \
  --event-title "NBA总决赛" \
  --market-question "湖人队是否会夺冠？" \
  --outcome "yes"
```

---

#### 7. `ask-llm` - 直接询问 LLM

向 LLM 提出任意问题，获取通用 AI 回答。

```bash
python scripts/python/cli.py ask-llm "分析当前加密市场趋势"
```

**参数说明：**

| 参数 | 类型 | 说明 |
|------|------|------|
| `user_input` | str | 你想问的问题 |

**使用示例：**

```bash
# 市场分析
python scripts/python/cli.py ask-llm "分析当前加密市场趋势"

# 交易策略
python scripts/python/cli.py ask-llm "在预测市场交易中需要注意什么风险？"

# 数据解读
python scripts/python/cli.py ask-llm "如何解读预测市场的价格波动？"
```

---

#### 8. `ask-polymarket-llm` - 结合市场数据询问 LLM

让 AI 结合当前 Polymarket 市场和事件数据回答问题。

```bash
python scripts/python/cli.py ask-polymarket-llm "我想交易哪些类型的市场？"
```

**参数说明：**

| 参数 | 类型 | 说明 |
|------|------|------|
| `user_input` | str | 你想问的问题 |

**与 `ask-llm` 的区别：**
- `ask-llm`: 通用问题回答
- `ask-polymarket-llm`: 会先获取最新的市场和事件数据，结合上下文回答

**使用示例：**

```bash
# 寻找交易机会
python scripts/python/cli.py ask-polymarket-llm "现在哪些市场最有交易价值？"

# 市场分类
python scripts/python/cli.py ask-polymarket-llm "当前有哪些加密货币相关的市场？"

# 投资建议
python scripts/python/cli.py ask-polymarket-llm "新手应该从哪些市场开始交易？"
```

---

### ⚙️ 市场创建与自动交易

#### 9. `create-market` - 创建新市场请求

使用 AI 帮你生成一个新市场的创建请求格式。

```bash
python scripts/python/cli.py create-market
```

**作用：**
- AI 分析当前趋势
- 生成一个有价值的市场提案
- 输出符合 Polymarket 格式的市场描述

**使用示例：**

```bash
# 生成市场提案
python scripts/python/cli.py create-market

# 输出示例：
# market_description: str = 美国是否会在2025年前通过联邦层面的加密货币监管法案？
# 此市场将追踪美国国会是否通过专门的加密货币监管法案...
```

---

#### 10. `run-autonomous-trader` - 运行自主交易代理

让 AI 系统完全自动地分析市场并执行交易。

```bash
python scripts/python/cli.py run-autonomous-trader
```

**执行流程：**

1. **数据获取**: 从 Polymarket 获取最新市场数据
2. **RAG 过滤**: 使用向量数据库筛选相关事件
3. **策略生成**: LLM 分析数据并生成交易策略
4. **交易执行**: 自动执行交易（需要配置钱包私钥）

**前提条件：**
- `.env` 中配置了 `POLYGON_WALLET_PRIVATE_KEY`
- 钱包中有足够的资金
- 已创建 RAG 数据库

**使用示例：**

```bash
# 运行自动交易
python scripts/python/cli.py run-autonomous-trader

# 系统会自动：
# - 扫描市场
# - 分析机会
# - 执行交易
# - 输出交易结果
```

⚠️ **风险提示**: 自动交易涉及真实资金，请谨慎使用！建议先在测试环境验证。

---

---

## 实战场景与工作流

### 场景一：市场探索与研究

**目标**: 了解当前有哪些活跃市场，找到感兴趣的交易机会。

```bash
# Step 1: 查看所有市场，按 spread 排序找出价差最大的
python scripts/python/cli.py get-all-markets --limit 20 --sort-by spread

# Step 2: 查看事件，了解有哪些主题类别
python scripts/python/cli.py get-all-events --limit 10

# Step 3: 搜索特定主题的相关新闻
python scripts/python/cli.py get-relevant-news "加密货币"

# Step 4: 结合 AI 分析市场趋势
python scripts/python/cli.py ask-polymarket-llm "总结当前最热门的3个交易主题"
```

**适用人群**: 新手用户、市场研究者

---

### 场景二：AI 辅助交易决策

**目标**: 在做出交易决定前，获取 AI 的分析和建议。

```bash
# Step 1: 找到感兴趣的市场
python scripts/python/cli.py get-all-markets --limit 10

# Step 2: 询问超级预测者对该市场的看法
python scripts/python/cli.py ask-superforecaster \
  --event-title "2024年美国大选" \
  --market-question "特朗普是否会获胜？" \
  --outcome "yes"

# Step 3: 获取更详细的分析
python scripts/python/cli.py ask-llm "分析影响2024美国大选的主要因素"

# Step 4: 结合新闻数据验证
python scripts/python/cli.py get-relevant-news "美国大选 民调"
```

**适用人群**: 有一定经验的交易者、想要第二意见的投资者

---

### 场景三：建立本地 RAG 数据库

**目标**: 构建本地向量数据库，实现快速检索和离线分析。

```bash
# Step 1: 创建本地市场 RAG 数据库（首次运行需要几分钟）
python scripts/python/cli.py create-local-markets-rag ./data/markets_db

# Step 2: 使用自然语言查询相关市场
python scripts/python/cli.py query-local-markets-rag ./data/markets_db "比特币价格预测"

# Step 3: 查询不同主题
python scripts/python/cli.py query-local-markets-rag ./data/markets_db "NBA总冠军"
python scripts/python/cli.py query-local-markets-rag ./data/markets_db "美联储利率"

# Step 4: 结合 AI 分析查询结果
python scripts/python/cli.py ask-polymarket-llm "基于RAG查询结果，推荐最有潜力的3个市场"
```

**适用人群**: 高级用户、量化交易者、需要批量分析的用户

---

### 场景四：完全自动化交易

**目标**: 设置 AI 代理自动扫描市场并执行交易。

```bash
# Step 1: 确保环境配置完整
# - 检查 .env 中的 POLYGON_WALLET_PRIVATE_KEY
# - 确认钱包有足够资金
# - 验证 LLM_PROVIDER 和 API keys

# Step 2: 创建 RAG 数据库（如果还没有）
python scripts/python/cli.py create-local-markets-rag ./data/rag_db

# Step 3: 运行自主交易代理
python scripts/python/cli.py run-autonomous-trader

# 系统将自动执行：
# - 获取最新市场数据
# - 使用 RAG 过滤相关事件
# - LLM 生成交易策略
# - 执行交易
```

⚠️ **重要提示**:

- 首次使用建议先在测试环境或小额资金验证
- 可以在代码中设置交易金额限制
- 建议定期检查交易历史和资金状况

**适用人群**: 高级用户、完全信任 AI 系统的交易者

---

### 场景五：创建新市场提案

**目标**: 使用 AI 生成有价值的预测市场提案。

```bash
# Step 1: 运行市场创建助手
python scripts/python/cli.py create-market

# Step 2: AI 会生成类似以下的提案：
# market_description: str = 美国是否会在2025年前通过联邦层面的加密货币监管法案？
# [详细的市场描述、解析规则、结算条件等]

# Step 3: 如果满意，可以将提案提交到 Polymarket
```

**适用人群**: 想要创建新市场的流动性提供者

---

## 常见问题 FAQ

### Q1: CLI 运行报错 `ModuleNotFoundError: No module named 'agents'`

**A**: 这个问题已经在最新代码中修复。如果仍然遇到：

```bash
# 确认在项目根目录
cd /Users/leyudame/Documents/agents

# 确认激活 conda 环境
conda activate polymarket

# 直接运行（cli.py 已自动处理路径）
python scripts/python/cli.py --help
```

---

### Q2: NewsAPI 返回空列表 `[]`

**A**: 可能的原因：

1. **API Key 未配置**: 检查 `.env` 文件中的 `NEWSAPI_API_KEY`
2. **关键词不匹配**: 尝试使用英文关键词，如 `"Bitcoin"` 而非 `"比特币"`
3. **API 限额**: NewsAPI 免费版有请求限制

```bash
# 测试 API 是否正常
python scripts/python/cli.py get-relevant-news "Bitcoin"
```

---

### Q3: LLM 调用失败或超时

**A**: 检查清单：

1. **确认 API Key 配置**:

```bash
# 检查 .env 文件
cat .env | grep API_KEY
```

2. **确认 Provider 匹配**:

```bash
# 如果使用 OpenAI
LLM_PROVIDER="openai"
OPENAI_API_KEY="sk-..."

# 如果使用 Gemini
LLM_PROVIDER="gemini"
GOOGLE_API_KEY="..."
```

3. **测试连接**:

```bash
python scripts/python/cli.py ask-llm "测试"
```

---

### Q4: `get-relevant-news` 返回空列表

**A**: NewsAPI 需要配置：

1. 注册 NewsAPI 账号: https://newsapi.org
2. 获取 API Key
3. 添加到 `.env`:

```bash
NEWSAPI_API_KEY="your_api_key_here"
```

---

### Q5: 如何切换不同的 LLM 提供商？

**A**: 修改 `.env` 文件：

```bash
# 使用 OpenAI
LLM_PROVIDER="openai"
OPENAI_API_KEY="sk-..."
LLM_MODEL="gpt-4"

# 使用 OpenRouter
LLM_PROVIDER="openrouter"
OPENROUTER_API_KEY="..."
LLM_MODEL="anthropic/claude-3-opus"

# 使用 Gemini
LLM_PROVIDER="gemini"
GOOGLE_API_KEY="..."
LLM_MODEL="gemini-pro"

# 使用 SiliconFlow（国内）
LLM_PROVIDER="siliconflow"
SILICONFLOW_API_KEY="..."
LLM_MODEL="Qwen/Qwen2.5-72B-Instruct"
```

---

### Q6: 如何限制交易金额？

**A**: 编辑交易相关代码，在执行前添加金额检查：

文件位置: `agents/application/trade.py`

```python
# 添加交易金额限制
MAX_TRADE_AMOUNT = 100  # 最大交易金额（美元）

# 在执行交易前检查
if trade_amount > MAX_TRADE_AMOUNT:
    print(f"交易金额 ${trade_amount} 超过限制 ${MAX_TRADE_AMOUNT}")
    return
```

---

### Q7: 自动交易系统安全吗？

**A**: 风险和建议：

**风险**:
- AI 预测不准确可能造成损失
- 智能合约风险
- API 调用延迟导致价格滑点

**建议**:
1. 从小额开始测试
2. 设置每日/每笔交易限额
3. 定期审查交易历史
4. 使用专门的交易账户，不要放入全部资金
5. 在测试网先验证流程

---

## 注意事项

### 🔐 安全性

1. **私钥管理**
   - 永远不要将私钥提交到 Git
   - 使用环境变量或密钥管理服务
   - 定期轮换密钥

2. **API 密钥**
   - 定期检查 API 使用情况
   - 为不同环境使用不同的密钥
   - 设置预算限制

3. **交易安全**
   - 先在测试网测试
   - 设置合理的交易限额
   - 保留足够的 Gas 费

### ⚖️ 合规性

1. **司法管辖限制**
   - Polymarket 禁止美国用户交易
   - 确认你的地区允许参与预测市场

2. **税务申报**
   - 保留所有交易记录
   - 咨询税务专业人士

### 📊 最佳实践

1. **风险管理**
   - 不要投入超过你能承受损失的资金
   - 分散投资，不要孤注一掷
   - 定期评估和调整策略

2. **数据管理**
   - 定期备份 RAG 数据库
   - 记录交易决策的理由
   - 追踪 AI 预测的准确率

3. **系统维护**
   - 定期更新依赖包
   - 监控 API 调用费用
   - 保持 .env 文件最新

---

## 进阶技巧

### 1. 创建定时任务

使用 `cron` 定期运行自动交易：

```bash
# 编辑 crontab
crontab -e

# 每天早上 9 点运行自动交易
0 9 * * * cd /Users/leyudame/Documents/agents && /opt/anaconda3/envs/polymarket/bin/python scripts/python/cli.py run-autonomous-trader >> logs/trader.log 2>&1
```

---

### 2. 批量查询市场

创建 shell 脚本批量执行命令：

```bash
#!/bin/bash
# save as batch_query.sh

# 查询不同主题的市场
python scripts/python/cli.py query-local-markets-rag ./data/db "加密货币"
python scripts/python/cli.py query-local-markets-rag ./data/db "政治选举"
python scripts/python/cli.py query-local-markets-rag ./data/db "体育赛事"

# 获取新闻
python scripts/python/cli.py get-relevant-news "Bitcoin"
```

---

### 3. 日志记录

在 `.env` 中添加日志配置：

```bash
# 启用详细日志
LOG_LEVEL="DEBUG"
LOG_FILE="logs/polymarket_agents.log"
```

---

## 相关资源

- **项目文档**: [CLAUDE.md](./CLAUDE.md)
- **Polymarket 官网**: https://polymarket.com
- **Polymarket API 文档**: https://docs.polymarket.com
- **LangChain 文档**: https://python.langchain.com
- **ChromaDB 文档**: https://docs.trychroma.com

---

## 获取帮助

如遇问题：

1. 查看本文档的"故障排查"部分
2. 检查 GitHub Issues
3. 查看 CLAUDE.md 了解项目架构
4. 确认环境配置正确
